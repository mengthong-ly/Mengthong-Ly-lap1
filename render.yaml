services:
  - type: web
    name: mengthong-ly-lab1
    env: node
    buildCommand: yarn install --frozen-lockfile
    startCommand: yarn start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
